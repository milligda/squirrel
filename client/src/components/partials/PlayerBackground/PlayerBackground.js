import React from "react";
import "./playerBackground.css";

const PlayerBackground = ({children}) => (
  <div className="player-background-container">{children}</div>
);

export default PlayerBackground;
